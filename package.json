{
  "name": "universe-lab",
  "version": "0.1.4.1.2",
  "private": true,
  "type": "module",
  "description": "Scientific mobile-first seeded universe laboratory with perceptual stellar rendering, speculative transit navigation, BOOST vector control, extreme objects, space weather, scientific overlays, cosmic phenomena, and particle experiments.",
  "scripts": {
    "check": "node ./tests/static-check.mjs && find src tests -type f \\( -name '*.js' -o -name '*.mjs' \\) -print0 | xargs -0 -n1 node --check",
    "test": "node --test tests/*.test.mjs",
    "qa": "npm run check && npm test"
  }
}
