{
  "name": "universe-lab",
  "version": "0.1.4",
  "private": true,
  "type": "module",
  "description": "Scientific mobile-first seeded universe laboratory with cosmic phenomena exploration, active compact-object visuals, physical comets, scalable particle experiments, and bounded-thrust navigation.",
  "scripts": {
    "check": "node ./tests/static-check.mjs && find src tests -type f \\( -name '*.js' -o -name '*.mjs' \\) -print0 | xargs -0 -n1 node --check",
    "test": "node --test tests/*.test.mjs",
    "qa": "npm run check && npm test"
  }
}
