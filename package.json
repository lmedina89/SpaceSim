{
  "name": "universe-lab",
  "version": "0.1.3.2.2",
  "private": true,
  "type": "module",
  "description": "Scientific mobile-first seeded solar-system laboratory with restored particle-warp runtime safety, isolated ship/observation rendering, scalable particle experiments, and physical experiment rendezvous navigation.",
  "scripts": {
    "check": "node ./tests/static-check.mjs && find src tests -type f \\( -name '*.js' -o -name '*.mjs' \\) -print0 | xargs -0 -n1 node --check",
    "test": "node --test tests/*.test.mjs",
    "qa": "npm run check && npm test"
  }
}
