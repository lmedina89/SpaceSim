{
  "name": "universe-lab",
  "version": "0.1.4.6",
  "private": true,
  "type": "module",
  "description": "Scientific mobile-first seeded universe laboratory with a canonical astronomical observer, continuous inertial sky across orbit/descent/surface/ascent, persistent planetary environments, guarded landing lifecycle, and iPhone/iPad WebKit WebGL2 compatibility.",
  "scripts": {
    "check": "node ./tests/static-check.mjs && find src tests -type f \\( -name '*.js' -o -name '*.mjs' \\) -print0 | xargs -0 -n1 node --check",
    "test": "node --test tests/*.test.mjs",
    "qa": "npm run check && npm test"
  }
}
