{
  "name": "universe-lab",
  "version": "0.1.1.3",
  "private": true,
  "type": "module",
  "description": "Scientific mobile-first seeded solar-system laboratory with targeting, N-body trajectory prediction, experiment launching, and hardened iOS continuous-flight controls.",
  "scripts": {
    "check": "node ./tests/static-check.mjs && find src tests -type f \\( -name '*.js' -o -name '*.mjs' \\) -print0 | xargs -0 -n1 node --check",
    "test": "node --test tests/*.test.mjs",
    "qa": "npm run check && npm test"
  }
}
