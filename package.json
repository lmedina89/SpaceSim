{
  "name": "universe-lab",
  "version": "0.1.4.6.1.3",
  "private": true,
  "type": "module",
  "description": "Scientific mobile-first seeded universe laboratory with a low-obstruction interactive 3D cockpit, compact mobile thrust controls, and an isolated speculative FRAME drive for target-directed astronomical travel.",
  "scripts": {
    "check": "node ./tests/static-check.mjs && find src tests -type f \\( -name '*.js' -o -name '*.mjs' \\) -print0 | xargs -0 -n1 node --check",
    "test": "node --test tests/*.test.mjs",
    "qa": "npm run check && npm test"
  }
}
