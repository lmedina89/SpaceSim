{
  "name": "universe-lab",
  "version": "0.1.4.6.1.2",
  "private": true,
  "type": "module",
  "description": "Scientific mobile-first seeded universe laboratory with canonical sky continuity plus a low-obstruction interactive 3D spacecraft cockpit with NAV/FLIGHT/SCIENCE instrumentation, live status lighting, and a ship-mounted SYSTEM DIAGNOSTICS MFD.",
  "scripts": {
    "check": "node ./tests/static-check.mjs && find src tests -type f \\( -name '*.js' -o -name '*.mjs' \\) -print0 | xargs -0 -n1 node --check",
    "test": "node --test tests/*.test.mjs",
    "qa": "npm run check && npm test"
  }
}
