{
  "name": "universe-lab",
  "version": "0.1.5.3.1",
  "private": true,
  "type": "module",
  "description": "Scientific mobile-first seeded universe laboratory with Newtonian N-body gravity, canonical planetary environment derivation, multi-world surface exploration, pressure-scaled atmosphere/sky optics, celestial appearance/eclipses, and an isolated speculative FRAME drive.",
  "scripts": {
    "check": "node ./tests/static-check.mjs && find src tests -type f \\( -name '*.js' -o -name '*.mjs' \\) -print0 | xargs -0 -n1 node --check",
    "test": "node --test tests/*.test.mjs",
    "qa": "npm run check && npm test"
  }
}
